server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: 10_zipkin_y_sleuth
          uri: https://github.com/Elimasrojas/microservicios
          search-paths: config-data
  application:
    name: config-service


