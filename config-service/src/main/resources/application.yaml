server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: 11_keycloak_part1
          uri: https://github.com/Elimasrojas/microservicios
          search-paths: config-data
  application:
    name: config-service


